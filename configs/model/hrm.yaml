model:
  name: hrm
  hidden_dim: 256
  input_dim: 64
  output_dim: 10

memory:
  enabled: false
  slots: 0
  slot_dim: 0
  reset_each_forward: false

halting:
  enabled: true
  max_segments: 10
  ponder_cost: 0.01
  threshold: 1.0

implicit_grad:
  k_steps: 3
